import { FilterKeywordPipe } from './filter-keyword.pipe';

describe('FilterKeywordPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterKeywordPipe();
    expect(pipe).toBeTruthy();
  });
});
